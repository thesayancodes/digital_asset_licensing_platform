[workspace]
resolver = "2"
members = [
    "asset-registry",
    "license-manager",
]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
debug = false
strip = "symbols"
panic = "abort"
overflow-checks = true

[profile.release-with-logs]
inherits = "release"
debug-assertions = true
